Purpose: Small utilities and shared helpers.

- `naming_utils.py`: filename sanitization and dated artifact path builder
- `config_manager.py`: config loader (env + YAML)
- `sanitize.js` / `debug-sanitize.js`: JS helpers for legacy shell usage

Add new shared helpers here instead of scattering them at top-level.
