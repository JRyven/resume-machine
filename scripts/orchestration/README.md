Purpose: Orchestration and batch-run scripts.

- `py_orchestrate_correlation_to_pdf.py`: single-job orchestrator
- `batch-process.sh`: batch workflow (adapter + generation)
- `preprocess-resume.js`: resume preprocessing step

Keep orchestration-level entrypoints here.
