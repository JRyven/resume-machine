# name: CI

# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.11'

#       - name: Install dependencies
#         run: python -m pip install --upgrade pip && python -m pip install -r requirements.txt

#       - name: Run tests
#         run: pytest -q

#       - name: Build (dry-run)
#         run: |
#           # Attempt a dry-run build; do not fail the job if content missing
#           python scripts/build_resume.py --base content/base.yaml --output-dir artifacts --dry-run || true

#       - name: Upload artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: artifacts
#           path: artifacts
